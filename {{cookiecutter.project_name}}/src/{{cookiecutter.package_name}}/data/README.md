# Package Data

This data folder is used to store all the data required to run the package.

The content of this data folder can be ignored by git (added to .gitignore) to keep source tracking efficient. The data can also be pulled from another data repository, e.g. via git LFS.
